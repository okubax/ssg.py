---
title: About
---

This is a page, not a post. Pages live in `content/pages/`, posts live in
`content/posts/`, and both are just Markdown files with a bit of YAML
front matter on top.

Edit this file, run `../ssg.py build`, and look in `output/about/`.
